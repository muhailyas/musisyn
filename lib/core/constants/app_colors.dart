import 'package:flutter/material.dart';

class AppColors {
  static const whiteColor = Colors.white;
  // static const primaryColor = Color.fromRGBO(116, 15, 46, 1);
  // static const secondaryColor = Color.fromRGBO(136, 48, 64, 1);

  static const primaryColor = Colors.black;
  static const secondaryColor = Colors.white10;
}
